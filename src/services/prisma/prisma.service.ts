import { PrismaClient } from '@prisma/client';

export class PrismaService extends PrismaClient {
	/**
	 * Just place holder, if in future want to using custom configuration
	 * e.g. using ssl certificate for postgres!
	 */
}
